# This pipeline is expected to fail
pipeline:
  name: independent-jobs
  enabled: true
  jobs:
    - name: job-1
      enabled: true
      steps:
        - name: failing-step
          enabled: true
          run: YYZ
    - name: job-2
      enabled: true
      steps:
        - name: successful-step
          enabled: true
          run: pwd
