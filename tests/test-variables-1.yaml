pipeline:
  name: capture-test
  enabled: true
  env:
    foo: bar
  jobs:
    - name: job
      enabled: true
      with:
        'foo foo': bar.bar
        BAD: .
      steps:
        - name: Get project version
          run: ./mvnw --batch-mode help:evaluate -Dexpression=project.version -q -DforceStdout | awk '{gsub(/^[ \t\r\n]+|[ \t\r\n]+$/, ""); print}' >> $VERSION
        - name: Echo project version
          run: echo $VERSION
        - name: echo-step-1
          run: echo $FOO
        - name: echo-step-2
          run: echo ${{ FOO }}
        - name: echo-step-3
          run: echo $INPUT_FOO_FOO
        - name: echo-step-4
          run: echo ${{ INPUT_FOO_FOO }}
        - name: echo-step-5
          run: echo $FOO ${{ INPUT_FOO_FOO }}
