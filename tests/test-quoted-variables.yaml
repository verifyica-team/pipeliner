pipeline:
  name: test-quoted
  with:
    test_scripts_directory: $PIPELINER_HOME/tests/scripts
    test_property: "test value"
  jobs:
    - name: test-quoted
      steps:
        - name: cleanup file
          run: rm -Rf $PIPELINER_TMP/ps-aux-root.txt
        - name: step-1
          run: ps aux | awk '{ print $1, $11}' | grep root > $PIPELINER_TMP/ps-aux-root.txt
        - name: cleanup file
          run: rm -Rf $PIPELINER_TMP/ps-aux-root.txt
        - name: step-2
          run: echo '$PIPELINER_TMP' "$PIPELINER_TMP"
        - name: step-3
          run: echo "$PIPELINER_TMP" '$PIPELINER_TMP'
        - name: step-4
          run: echo '${PIPELINER_TMP}' "$PIPELINER_TMP"
        - name: step-5
          run: echo "$PIPELINER_TMP" '${PIPELINER_TMP}'
        - name: test-6
          run: echo "'$PIPELINER_TMP'" '$PIPELINER_TMP'
        - name: test-7
          run: echo '"$PIPELINER_TMP"' "$PIPELINER_TMP"
        - name: test-8
          run: echo \"\${{ test_property }}\" \"${{ test_property }}\"
        - name: test-9
          with:
            test_property_1: \${PWD}
            test_property_2: $PWD
            test_property_3: ${PWD}
            test_property_4: FOO_ ${{ test_property_2 }} _BAR
            test_property_5: FOO_ ${{ test_property_2 }} ${{ test_property_3 }} _BAR
          run: |
            echo \"\${{ test_property_1 }}\" \"${{ test_property_1 }}\" \"${{ test_property_2 }}\" \"${{ test_property_3 }}\" \"${{ test_property_4 }}\"
            ${{test_scripts_directory}}/test-arguments-are-equal.sh "${{ test_property_1 }}" "\${PWD}"
            ${{test_scripts_directory}}/test-arguments-are-equal.sh "${{ test_property_2 }}" "$PWD"
            ${{test_scripts_directory}}/test-arguments-are-equal.sh "${{ test_property_2 }}" "${PWD}"
            ${{test_scripts_directory}}/test-arguments-are-equal.sh "FOO_ $PWD _BAR" "${{ test_property_4 }}"
            ${{test_scripts_directory}}/test-arguments-are-equal.sh "FOO_ $PWD ${PWD} _BAR" "${{ test_property_5 }}"
