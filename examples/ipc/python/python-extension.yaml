pipeline:
  name: Hello World Python Pipeline
  jobs:
    - name: Hello World Python Job
      working-directory: examples/ipc/python
      steps:
        - name: Hello World Python Step
          shell: none
          with:
            name: Test User
          run: python3 extension.py
        - name: Hello World Python Captured Variables
          run: |
            echo captured extension variable \${{ extension_variable_1 }} = "${{ extension_variable_1 }}"
            echo captured extension variable \${{ extension_variable_2 }} = "${{ extension_variable_2 }}"
